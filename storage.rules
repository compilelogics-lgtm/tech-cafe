rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // 🔓 Allow all reads/writes for any authenticated user during dev
      allow read, write: if request.auth != null;
    }
  }
}
